"""Concrete agent implementations (Sprint 2+). Each subclasses BaseAgent and
overrides build_context / apply to read and write its real target tables.
Registered in app/agents/registry.py:AGENT_CLASSES."""
